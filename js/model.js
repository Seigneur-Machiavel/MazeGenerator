function buildModel() {
   const model = {
       shape: 'square',
       mask: {},
       algorithmDelay: 0,
       exitConfig: 'vertical'
   };

    return model;
}
module.exports = {
    buildModel
}